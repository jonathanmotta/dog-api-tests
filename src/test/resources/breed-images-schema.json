{
  "type": "object",
  "properties": {
    "status": {
      "type": "string",
      "enum": ["success"]
    },
    "message": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^https://"
      }
    }
  },
  "required": ["status", "message"]
}
